<h1>ingresaste</h1>
<div class="container">
    <div class="card" style="width: 18rem;">
        <img src="https://i.pravatar.cc/150?img=5" class="rounded-circle img-thumbnail" alt="Profile Picture">
        <div class="card-body">
            <h5 class="card-title">{{user.firstName}} {{user.lastName}} </h5>
            <p class="card-text">Email : {{user.email}}</p>
            <p class="card-text">Rol : {{user.role}}</p>

        </div>
    </div>
    <a href="/api/sessions/logout">Logout</a>
</div>

